package ca.floo.roadtrip.service.notification.common

/**
 * What the notification copy calls a booking site it cannot name: a fire that
 * reached no booking adapter, or one alert whose campgrounds are sold by
 * different vendors. Naming the first one found would be wrong for the rest.
 */
object NeutralBookingCopy {
    const val BOOKING_SITE = "the booking site"
}
