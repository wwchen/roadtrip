{
  "name": "roadtrip-qa",
  "private": true,
  "version": "0.0.0",
  "description": "Local-only Playwright smoke for the roadtrip webapp. See ../Makefile (make qa).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
