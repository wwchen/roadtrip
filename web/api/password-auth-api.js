// web/api/password-auth-api.js
//
// Stub — real implementation wired in Task 8.
// This file exists only so that login-card.js can import it at module load
// time without throwing; the login-card tests always inject _completeLogin,
// so this export is never called in test runs.

export function completePasswordLogin() {
  throw new Error('completePasswordLogin: wired in Task 8');
}
